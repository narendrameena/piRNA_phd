channels:
  - conda-forge
  - bioconda

dependencies:
  - python=3.12.3
  - numpy
  - pandas
  - matplotlib
  - pysam
  - biopython
  - pip
  - pip:
      - tstk  # only if you're installing from PyPI or a local wheel